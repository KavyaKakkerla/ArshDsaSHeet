class Solution {
    public int findDuplicate(int[] nums) {
        for(int n:nums)
        {
            int num=Math.abs(num);
            if()
        } 
        return -1;      
    }
}
 int len = nums.length;
        for (int num : nums) {
            int idx = Math.abs(num);
            if (nums[idx] < 0) {
                return idx;
            }
            nums[idx] = -nums[idx];
        }

        return len;
